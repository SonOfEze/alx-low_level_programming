#include "main.h"

/**
 * _islower - checks if the character is lowewrcase
 * @c: this is a character
 * Return: return 1 and 0 pending on condition
 */

int _islower(int c)
{

	return (c >= 'a' && c <= 'z');
}
